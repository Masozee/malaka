To run the backend tests, navigate to the `backend` directory and execute the following command:

```bash
go test ./...
```

This command will run all tests within the `backend` module and its sub-packages.